import { Card } from '@/components/ui/card';import { Button } from '@/components/ui/button';import { rides } from '@/mock/rides';import { drivers } from '@/mock/drivers';import { peso } from '@/lib/utils';
export default function Driver(){const driver=drivers[0];return <div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><Card><h1 className="text-3xl font-black">Driver workspace</h1><p className="mt-2 text-slate-600">{driver.name} · {driver.plate}</p><div className="mt-5 rounded-2xl bg-green-50 p-4"><b>Status:</b> Available near {driver.barangay}</div><Button className="mt-4 w-full">Go online</Button></Card><Card><h2 className="font-bold">Today&apos;s trips</h2>{rides.map(r=><article key={r.id} className="mt-4 rounded-2xl border p-4"><p className="font-semibold">{r.pickup} → {r.dropoff}</p><p className="text-sm text-slate-600">{r.status.replace('_',' ')} · {peso(r.fare)}</p></article>)}</Card></div>}
